model EmailLog {
  id             Int     @id @default(autoincrement())
  uid       String   @unique
  sender    String
  receiver  String
  subject   String
  html      String
  status    String
  messageId String?  @map("message_id")
  response  String?
  timestamp DateTime @default(now())
  
  @@map("email_logs")
}