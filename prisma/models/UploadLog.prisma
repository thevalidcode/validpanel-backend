model UploadLog {
  id             Int     @id @default(autoincrement())
  uid       String   @unique
  size      Int
  url       String
  filename  String
  mimetype  String
  timestamp DateTime @default(now())

  @@map("upload_logs")
}